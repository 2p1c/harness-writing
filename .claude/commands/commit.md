---
description: '分析改动并生成规范化提交。'
---

# /commit

Invoke skill `git-commit` immediately.

If the user provides extra text after `/commit`, treat it as task context and execute with this skill workflow.
