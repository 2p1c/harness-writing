---
description: '按 Git Flow 规则创建并切换分支。'
---

# /branch

Invoke skill `git-flow-branch-creator` immediately.

If the user provides extra text after `/branch`, treat it as task context and execute with this skill workflow.
