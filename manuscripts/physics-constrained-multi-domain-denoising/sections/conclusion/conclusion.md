## 主要贡献总结

本文对激光超声无损检测做出以下四项主要贡献：

1. **PMDF-Net多域融合架构。** 一种双分支编码器-解码器，联合处理时域波形与时频表示，提取互补特征以增强信号去噪保真度。

2. **物理约束损失函数。** 一种保特征损失 formulation，强制执行到达时间、振幅与波形形态一致性，确保去噪信号保留用于缺陷表征的诊断信息。

3. **减平均训练协议。** 一种配对训练策略，使用低平均次数输入 ($N=16$) 对抗高平均次数目标 ($M=256$)，使实际部署无需在检测过程中进行大量信号平均。

4. **跨域泛化能力。** 展示了从完整铝试样到含缺陷工件的跨域迁移，以及跨厚度变化的泛化，验证了该方法在训练条件之外的真实检测场景中的适用性。

这些贡献确立了一个高效激光超声检测框架，在减少平均次数需求的同时，保留并超越了传统平均方法在缺陷检测任务中的性能。

## 主要发现

PMDF-Net实现了峰值信噪比(SNR)提升达 11.4 dB，同时以高于竞争方法的保真度保留诊断信号特征。这一性能直接转化为实际工作流程收益：16 倍的信号平均次数减少（16 次对 256 次平均）使得在满足工业检测吞吐量要求的采集时间内实现亚表面缺陷检测成为可能。关键在于，仅在完整试样数据上训练的模型能够泛化至含缺陷测试件而无需任务特定微调，展示了强大的零样本迁移能力。消融实验证实，多域融合与物理约束分支二者缺一不可——移除任一组件均导致信噪比(SNR)下降超过 3 dB。这些结果确立了PMDF-Net作为一条可行路径，使激光超声检测摆脱表面耦合要求的制约，并克服传统基于平均方法的吞吐量瓶颈。

## 局限性与未来方向

该方法的主要局限性在于配对训练数据需求以及当前仅覆盖单一材料（铝）的范围；跨厚度泛化仍存在差距，训练厚度范围外的试样可观测到信噪比(SNR)明显下降。未来工作应通过领域自适应实现多材料训练、采用自监督去噪以降低配对数据依赖，以及在嵌入式平台上实现实时部署。PMDF-Net展示了物理约束多域融合在实用激光超声检测中的潜力。
